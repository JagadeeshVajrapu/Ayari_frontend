import { AddressesView } from '@/features/account/components/addresses-view';

export const metadata = { title: 'Addresses — AYARI' };

export default function AddressesPage() {
  return <AddressesView />;
}
