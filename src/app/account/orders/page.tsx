import { OrdersView } from '@/features/account/components/orders-view';

export const metadata = { title: 'Orders — AYARI' };

export default function OrdersPage() {
  return <OrdersView />;
}
