import { ProfileView } from '@/features/account/components/profile-view';

export const metadata = { title: 'Profile — AYARI' };

export default function ProfilePage() {
  return <ProfileView />;
}
