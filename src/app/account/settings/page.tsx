import { SettingsView } from '@/features/account/components/settings-view';

export const metadata = { title: 'Settings — AYARI' };

export default function SettingsPage() {
  return <SettingsView />;
}
