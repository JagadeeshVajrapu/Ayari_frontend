import { RegisterForm } from '@/features/auth/components/register-form';

export const metadata = {
  title: 'Create Account — AYARI',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
