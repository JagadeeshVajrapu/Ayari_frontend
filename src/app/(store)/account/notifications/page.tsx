import { NotificationsView } from '@/features/account/components/notifications-view';

export const metadata = { title: 'Notifications — AYARI' };

export default function NotificationsPage() {
  return <NotificationsView />;
}
