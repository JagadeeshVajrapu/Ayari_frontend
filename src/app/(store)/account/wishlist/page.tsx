import { WishlistView } from '@/features/account/components/wishlist-view';

export const metadata = { title: 'Wishlist — AYARI' };

export default function WishlistPage() {
  return <WishlistView />;
}
